import React from 'react';

const ThemeContext = React.createContext('blue');

//here we create a context called ThemeContext, that can be used
//in other components
//some initial value is enough

export default ThemeContext;
